{{ theme['css_scripts'] }}
.unsubscribe-section { 
    margin: 0 !important;
}


.login-form {
    .form-field {
        &:first-child {
            &::before {
                content: "{{ 'VARIABLE.EMAIL'|t }}";
            }
        }

        &:nth-child(2) {
            &::before {
                content: "{{ 'VARIABLE.PASSWORD'|t }}";
            }
        }
    }
}